const fs = require('fs'); let code = fs.readFileSync('src/App.tsx', 'utf8'); code = code.substring(0, code.indexOf('export default App;') + 'export default App;'.length) + '\n'; code = code.replace(/<nav className=\{[\s\S]*?\}>/, '<nav className={ixed w-full z-50 transition-all duration-300 }>'); code = code.replace(/<span className=\{[\s\S]*?\}>Paradise Crew<\/span>/, '<span className={	ext-2xl font-bold tracking-tight }>Paradise Crew</span>'); code = code.replace(/<span className=\{[\s\S]*?\}>Tech & Hospitality<\/span>/, '<span className={	ext-xs tracking-widest font-medium uppercase }>Tech & Hospitality</span>'); code = code.replace(/<div className=\{hidden md:flex items-center space-x-8 \}>/, '<div className={hidden md:flex items-center space-x-8}>'); code = code.replace(/className=\{\\\$\\{isScrolled \\\? \\'text-ocean-900\\' : \\'text-ocean-900\\'\\}\\}/, 'className={md:hidden }'); fs.writeFileSync('src/App.tsx', code);
